
## 一、MIRAGE 

| 层级                     | 交互特征                                                     | 对 SOP 的启示                          |
| ------------------------ | ------------------------------------------------------------ | -------------------------------------- |
| **第一层：专家如何诊断** | 专家的回答不是一次性给出所有信息，而是**根据用户的信息完整度动态调整** | SOP 需要预设不同信息条件下的回答策略   |
| **第二层：专家如何追问** | 专家对话中包含大量**主动追问**（如“你看到叶片有黄点吗？”），而不是被动等待用户提供完整信息 | SOP 需要预设追问点，而非仅提供标准答案 |
| **第三层：专家如何决策** | 专家的每一步决策都基于**累积的对话信息和专业知识的结合**     | SOP 需要拆分诊断条件，而非仅罗列措施   |


## 二、MIRAGE 评估的核心能力

MIRAGE 设计了两个互补的子任务来评估模型，**这些能力直接对应了专家应用 SOP 时的智能行为**：

**MMST (Single-Turn) 评估的能力**：
- **识别（Identify）**：从给定的图像和用户问题中，识别相关实体（作物、病害、虫害）→ 对应 SOP 中的 **“症状识别”模块**
- **推理（Infer）**：推断因果关系和诊断逻辑 → 对应 SOP 中的 **“诊断逻辑”模块**
- **生成（Generate）**：给出可执行的、专家级的建议 → 对应 SOP 中的 **“操作步骤”模块**

**MMMT (Multi-Turn) 评估的能力**：
- **对话状态追踪（Dialogue State Tracking）**：记住对话历史中已获得和仍缺失的信息 → 对应 SOP 中的 **“关键信息缺口”列表**
- **目标驱动生成（Goal-driven Generation）**：每轮回答始终朝着“解决问题”的目标推进 → 对应 SOP 中的 **“信息完整度-回答类型”映射**
- **专家级对话决策**：判断何时该追问、何时可以给出完整答案 → 对应 SOP 中的 **“默认追问策略”**


## 三、MIRAGE 与 SOP 的核心逻辑映射（为什么这样设计）

如果你的 SOP 只包含“症状+对策”（静态），模型**永远无法学到**多轮交互中的追问和决策能力。SOP 必须围绕 MIRAGE 提出的三层评估模式来设计：

| MIRAGE 评估能力  | 对应的 SOP 设计要素                                          | 为什么这样设计？                                             |
| ---------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 识别（Identify） | 诊断条件模块：标准症状（分类识别） + 关键疑点（边界情况提醒） | 让模型知道什么症状对应什么病害，以及在疑难情况下需要额外信息 |
| 推理（Infer）    | 诊断条件模块：诊断逻辑 + 置信度分级                          | 让模型的推理过程可追溯、可解释                               |
| 生成（Generate） | 应答矩阵模块：按信息完整度分层的回答                         | 让模型在不同情况下给出恰当的回答类型                         |
| 对话状态追踪     | 信息缺口模块：关键/可选信息分类 + 追问话术                   | 让模型知道自己还缺什么信息、该问什么                         |
| 目标驱动生成     | 应答矩阵模块：回答类型与完整度绑定                           | 让模型知道“缺A信息→给部分方案+A追问”这种策略                 |
| 专家级对话决策   | 应答矩阵模块：默认回答（兜底策略）                           | 避免模型在信息严重不足时强行给出结论                         |


## 四、MIRAGE 风格的 SOP 构建指南（实践版）

基于以上框架，构建 SOP 的核心原则是：**从“对话中的决策节点”倒推知识拆分方式。**

### 5.1 信息缺口识别

真实农业咨询中，用户提问常极度简略，模型需要知道 **“还缺什么信息才能给出可靠答案”**。

**关键信息**：没有这些信息，**无法**给出准确诊断和操作建议。
**可选信息**：有这些信息可以**优化**答案（如用药轮换、品种推荐），但缺少时仍可给出基础方案。

### 5.2 应答策略映射

当信息完整度不同时，专家的回答类型会随之变化。构建 SOP 时，需预设以下回答类型：

| 信息完整度        | 建议的回答类型  | 说明                           |
| ----------------- | --------------- | ------------------------------ |
| 严重不足          | 追问            | 只反问，不给任何方案           |
| 部分信息 + 可推断 | 部分方案 + 追问 | 给出可确定的措施，追问缺失信息 |
| 信息充分          | 完整方案        | 直接给出全部操作               |
| 有特殊情况        | 备选方案        | 给出替代方案                   |

### 5.3 MIRAGE 风格 SOP 结构设计

基于上述推导，完整的 SOP 结构可设计为：

```
1. meta（元信息）
   - 作物、品种、生育期、来源、版本

2. diagnosis_criteria（诊断条件）
   ├── symptoms（标准症状）
   ├── triggers（发病诱因）
   ├── reasoning_chain（诊断逻辑）
   └── confidence_level（置信度分级）

3. missing_info_strategy（信息缺口策略）
   ├── key_entities（关键信息清单 + 追问话术）
   └── optional_entities（可选信息清单 + 追问话术）

4. response_matrix（应答矩阵）
   ├── scenarios（不同条件组合下的回答）
   │   ├── condition_checks（条件判断）
   │   ├── response_type（回答类型）
   │   ├── content（回答内容）
   │   └── fallback_strategy（兜底策略）
   └── default_response（默认回答：信息不足时使用）
```


## 五、项目适配方案

针对你的项目，建议采用 **“先轻量后扩展”** 的策略：

**第一步：当前阶段**
- 只建 `meta`（元信息）和 `diagnosis_criteria`（诊断条件）
- 核心目标是：用 `symptoms`（标准症状）+ `reasoning_chain`（诊断逻辑）保证生成的口语化问题和答案有事实依据
- 这一个模块就能覆盖 MIRAGE MMST 的评估内容

**第二步：中期扩展**
- 增加 `missing_info_strategy`（信息缺口）中的 `key_entities`（关键信息）
- 核心目标是：支撑多轮对话中的“主动追问”能力
- 每一个追问点可以来源于 SOP 原文的“常见问题”部分，需要人工补充

**第三步：长期完善**
- 逐步构建 `response_matrix`（应答矩阵）中的多条件分支，支撑更复杂的对话决策
- 这一步不急于完成，可以在数据集积累到一定规模后分批补充

## 六、从文本中提取 MIRAGE 风格 SOP 的提示词

下面是一套可直接使用的提示词，专为从农业技术文档（如病虫害防治图谱、种植规程）中提取结构化 SOP 而设计。它遵循 MIRAGE 理念，聚焦于支撑多轮对话的信息缺口与应答策略，同时保持可操作性。

---

### 提示词

```text
你是一名农业 SOP 抽取专家。你的任务是从给定的甘蔗种植技术文本中，提取出符合 MIRAGE 标准的多轮对话式 SOP。输出格式必须是严格的 JSON，遵循下面定义的 Schema。

## Schema 定义

{
  "meta": {
    "title": "SOP 标题（从原文归纳，如“甘蔗梢腐病防控”）",
    "crop": "作物（固定为“甘蔗”）",
    "variety_suitability": ["适用品种列表，原文未提及则为空数组"],
    "growth_stage": "适用生育期（如“拔节期”）",
    "source": "文档名称"
  },
  "diagnosis_criteria": {
    "symptoms": ["症状1原文摘录", "症状2原文摘录"],
    "triggers": ["诱因1", "诱因2"],
    "reasoning_chain": "一段总结性文字，解释如何从症状+诱因推断出该问题。例如：如果观察到 X 且 Y，则可判定为 Z。",
    "confidence": "高/中/低（基于症状典型程度，原文未提及则根据信息完整度合理推断）"
  },
  "missing_info_strategy": {
    "key_entities": [
      {
        "name": "信息名（如“病情阶段”）",
        "type": "categorical/boolean/text",
        "options": ["选项1", "选项2"] （当 type 为 categorical 时必填）,
        "description": "原文中该信息的描述",
        "why_important": "为什么需要这个信息（根据原文推断）"
      }
    ],
    "optional_entities": [
      {
        "name": "信息名（如“用药历史”）",
        "type": "text",
        "description": "原文描述",
        "why_important": "为什么需要这个信息"
      }
    ],
    "clarification_templates": {
      "信息名": "追问话术（如“发病是刚开始还是已经烂心了？”）"
    }
  },
  "response_matrix": {
    "scenarios": [
      {
        "condition_checks": {
          "信息名": "条件值或条件范围（如“初期”或[\"中期\",\"严重期\"]）"
        },
        "response_type": "complete_solution / partial_solution_with_clarification / alternative_solution / clarification_only",
        "content": "对应的回答内容（基于原文对策改写，保留数字和药名）",
        "follow_up_question": "如果是 partial_solution，需要追问的问题（可选）",
        "fallback_strategy": "如果该方案无效的备选建议（原文有则写，无则 null）"
      }
    ],
    "default_response": {
      "content": "当没有任何关键信息时的默认回答（通常是追问）",
      "response_type": "clarification_only"
    }
  }
}

## 提取规则（严格遵守）

1. **只抽取原文明确提到的内容**。如果原文没有某个字段的信息，则输出空字符串 ""、空数组 [] 或 null。
2. **不编造任何数据**：尤其不能编造药剂名、浓度、数字、品种。
3. **症状和诱因**：直接摘录原文中的短句，不要改写。
4. **key_entities**：只包含那些没有就无法准确诊断的信息（如病情阶段、环境湿度）。从原文的“诊断要点”“常见问题”中归纳。
5. **clarification_templates**：根据缺失信息的重要性，用自然语言编写一句农民能听懂的追问。如果原文没有现成话术，请根据常识合理生成（但不要添加新知识）。
6. **response_matrix.scenarios**：至少构建一个场景。优先使用原文中明确提到的不同条件（如“发病初期”vs“严重期”，“多雨”vs“干旱”）。如果原文没有明确的条件分支，则根据经验创建最可能的一个完整场景。
7. **default_response**：必须是一个简单的追问，引导用户提供关键信息。

## 输入文本

以下是需要提取的农业技术文档内容：

"""
{在此处粘贴文档原文}
"""

## 输出要求

只输出一个 JSON 对象，不要有任何额外的解释或标记。确保 JSON 格式合法。
```

---

### 示例输入（以梢腐病段落为例）

**原文**：
> 甘蔗梢腐病主要发生在拔节期，高温高湿条件下易发。症状：心叶出现黄褐色小斑，后扩展为梭形或长条形，中央灰白色，边缘暗红，严重时心叶腐烂有臭味。防治：发病初期用10%苯醚甲环唑1000倍液对准心叶喷雾，每亩30-40公斤水，每隔7天一次，连续2次。同时及时剥除病叶。若已用过三唑类药剂无效，可换用75%百菌清500倍液。注意：发病期间禁止追施尿素。

**模型输出**（符合上述Schema）见之前构建的示例，此处不重复。

---

好的，我们以**甘蔗梢腐病**为例，严格遵循刚刚设计的 MIRAGE 风格 SOP 结构，构建一个完整、可直接使用的实例。这个实例将展示如何在静态文本中嵌入多轮对话所需的逻辑，而不引入过度复杂的依赖。

---

## 基于 MIRAGE 理念的甘蔗梢腐病 SOP 实例

```json
{
  "meta": {
    "title": "甘蔗梢腐病防控标准操作程序",
    "crop": "甘蔗",
    "variety_suitability": ["桂糖42", "桂糖44", "粤糖93/159"],
    "growth_stage": "拔节期",
    "source": "甘蔗病虫害防治图谱 + 贵港大岭基地糖蔗种植规程",
    "version": "1.0"
  },
  "diagnosis_criteria": {
    "symptoms": [
      "心叶出现黄褐色小斑",
      "病斑扩展为梭形或长条形，中央灰白色，边缘暗红色",
      "严重时心叶腐烂，有臭味"
    ],
    "triggers": [
      "高温高湿（气温25-32℃，相对湿度>85%）",
      "偏施氮肥导致植株柔嫩",
      "种植密度过大，田间郁蔽"
    ],
    "reasoning_chain": "如果观察到心叶黄褐色斑且扩展后呈梭形/长条形（中央灰白边缘暗红），同时近期高温多湿或偏施氮肥，则可判定为梢腐病。",
    "confidence": "高（症状典型时）| 中（仅叶片症状无腐烂时）"
  },
  "missing_info_strategy": {
    "key_entities": [
      {
        "name": "病情阶段",
        "type": "categorical",
        "options": ["初期（刚见小斑）", "中期（斑扩展但未腐烂）", "严重期（心叶腐烂）"],
        "why_important": "决定是否需要拔除病株及施药强度"
      },
      {
        "name": "环境湿度",
        "type": "boolean",
        "description": "是否连续阴雨或田间积水",
        "why_important": "影响药剂选择及是否需要开沟排水"
      }
    ],
    "optional_entities": [
      {
        "name": "用药历史",
        "type": "text",
        "description": "近期是否用过三唑类药剂",
        "why_important": "避免抗药性，指导轮换用药"
      },
      {
        "name": "品种",
        "type": "categorical",
        "options": ["桂糖42", "桂糖44", "其他"],
        "why_important": "不同品种抗病性差异，影响后续品种调整建议"
      }
    ],
    "clarification_templates": {
      "病情阶段": "发病是刚开始只有零星小斑，还是已经大面积烂心了？",
      "环境湿度": "最近雨水多吗？地里有没有积水？",
      "用药历史": "你之前打过什么药？打过几次？",
      "品种": "你种的是桂糖哪个品种？"
    }
  },
  "response_matrix": {
    "scenarios": [
      {
        "condition_checks": {
          "病情阶段": "初期（刚见小斑）",
          "环境湿度": "较低（无连续阴雨）"
        },
        "response_type": "complete_solution",
        "content": "你现在处于发病初期，而且天气有利。请立即喷施10%苯醚甲环唑1000倍液，重点喷心叶，每亩30-40公斤水。隔7天再喷一次，共2次。同时把病叶剥掉带出地。",
        "fallback_strategy": null
      },
      {
        "condition_checks": {
          "病情阶段": ["中期", "严重期"],
          "环境湿度": "高（连续阴雨或积水）"
        },
        "response_type": "partial_solution_with_clarification",
        "content": "病情较重且湿度大，先排干积水，把严重烂心的病株拔掉。另外，雨停间歇立即喷药，用苯醚甲环唑1000倍液，可适当加到800倍。",
        "follow_up_question": "拔掉病株后，你观察一下周围植株还有没有新病斑出现？",
        "fallback_strategy": "如果一周后仍未控制，换用75%百菌清500倍液，并咨询当地农技站"
      },
      {
        "condition_checks": {
          "病情阶段": "any",
          "用药历史": "已用过三唑类药剂（如苯醚甲环唑）且无效"
        },
        "response_type": "alternative_solution",
        "content": "考虑到你已经用过三唑类药没效果，建议换用不同机理的药：75%百菌清500倍液或50%多菌灵800倍液。同时增施磷钾肥，暂停尿素。",
        "fallback_strategy": "若仍无效，可能是赤腐病混淆，请拍清晰照片送到农技站鉴定。"
      }
    ],
    "default_response": {
      "content": "请先告诉我发病是刚开始还是已经烂心了？最近雨水多吗？这能帮我判断用哪种方案。",
      "response_type": "clarification_only"
    }
  }
}
```

---

## 这个 SOP 如何支撑多轮对话？

### 场景模拟

**农民第一轮**：  
> “老师，甘蔗叶子黄了，有斑点，咋办？”

系统根据 `default_response`，因为没有获得任何关键实体信息，直接反问：  
> “请先告诉我发病是刚开始还是已经烂心了？最近雨水多吗？”

**农民第二轮**：  
> “刚开始有斑点，没烂。最近一直下雨。”

系统现在匹配到 `response_matrix` 中第一个场景（初期 + 湿度高？实际上湿度高匹配第二个场景？这里需要调整逻辑——当前案例中，初期+湿度高没有完全匹配的场景，但我们可以通过 `condition_checks` 的灵活匹配机制处理。为了演示，假设系统匹配第二个场景的部分条件，给出 `partial_solution_with_clarification`）：

> “病情刚发生但湿度大，容易加重。你现在先把积水排掉，然后在雨停的间歇喷药：苯醚甲环唑1000倍液，重点喷心叶。隔7天再喷一次。另外，剥掉病叶。”

并追问：  
> “你之前打过药吗？”

**农民第三轮**：  
> “没打过药。”

系统得到完整信息，给出最终方案（匹配第一个场景的完整条件）：  
> “很好，你现在立即按我说的喷药，7天一次，连喷2次。注意控制氮肥，增施磷钾肥。下次种选抗病品种如桂糖42。”

Q的随机性（表述方式）语言困惑度和mirage的语言困惑度（比较以证明数据有效）   （这是后面要做的
QA需要与图绑定 简单Q 通过（绑定）得到复杂A   ：规模化生成有实际依据的QA     （这是另一种实现方式
虽然模型能力整体更强但是不能够按照结构输入 所以不能够      （这是最后汇报可能要说的我们选择此种方式的原因