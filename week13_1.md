## 修改后的SOP抽取提示词（兼容多轮对话，不增加复杂度）

您原来的抽取提示词已经很好。为了让SOP更好地支撑后续可能的多轮对话（专家追问），在不大幅增加复杂度的前提下，增加一个 **`clarification_points`** 字段。这个字段只需从原文中抽取**那些非必要但有利于精准诊断的信息点**（例如病情严重程度、用药历史、天气条件、品种信息等），不需要结构化追问话术。

修改后的输出JSON结构如下：

```json
{
  "title": "甘蔗梢腐病防控SOP",
  "scope": "适用范围：甘蔗拔节期，高温高湿地区",
  "symptoms": "心叶黄褐色小斑，扩展后梭形或长条形，中央灰白色，边缘暗红，严重时心叶腐烂有臭味",
  "triggers": "高温高湿、偏施氮肥、田间郁蔽",
  "steps": [
    "及时剥除老叶、病叶，带出田外集中销毁，增加通风透光",
    "发病初期用10%苯醚甲环唑1000倍液对准心叶均匀喷雾，每亩30-40公斤水，每隔7天一次，连续2次",
    "下一季选用抗病品种如桂糖42号、粤糖93/159"
  ],
  "precautions": "发病期间禁止追施尿素；增施磷钾肥；喷药避开中午高温；阴雨天抢停雨间隙施药",
  "verification": "第二次施药后5天观察心叶恢复情况，无新病斑",
  "clarification_points": [
    "发病范围（零星几棵还是全田蔓延）",
    "是否已经用过药（用过什么药、用了几次）",
    "近期天气（是否连续阴雨）",
    "种植品种"
  ],
  "source": "甘蔗病虫害防治图谱"
}
```

### 修改后的提示词（可直接使用）

```text
你是一台农业SOP抽取机。你的任务是从甘蔗文本中提取标准操作程序，并标注关键追问点（用于未来多轮对话）。

规则（严格遵守）：
1. 只抽取原文明确提到的内容，绝不编造任何药剂、浓度、数字、品种。
2. 如果某个字段在原文中找不到，就输出空字符串或空数组。
3. 操作步骤必须保留原文中的数字（如“7天一次”“1000倍液”）。
4. 输出格式必须是严格的JSON数组，每个元素是一个SOP对象。

SOP对象结构：
{
  "title": "甘蔗XXX防控/操作SOP",
  "scope": "适用范围（作物+生育期+条件）",
  "symptoms": "症状识别特征（原文摘录）",
  "triggers": "发生条件/诱因",
  "steps": ["步骤1原文", "步骤2原文", ...],
  "precautions": "注意事项（若无则空字符串）",
  "verification": "记录与验证方法（若无则空字符串）",
  "clarification_points": ["关键追问点1", "关键追问点2", ...],
  "source": "文件名"
}

**关于 clarification_points 的说明**：从原文中抽取出那些**有助于精准诊断但农民可能遗漏的信息**，例如：
- 发病范围（零星/大面积）
- 是否用药、用过什么药
- 近期天气（降雨、湿度）
- 品种信息
- 土壤情况
- 施肥历史
等。每个追问点用一句简短的自然语言描述，不要编造。

原文：
{text}

请开始抽取。只输出JSON数组，不要额外解释。
```

1. **保持简洁**：仅增加一个数组字段，不引入嵌套结构、依赖关系、追问话术。  
2. **从原文中可抽取**：`clarification_points` 中的信息经常出现在原文的“注意事项”“常见问题”或“诊断要点”里。例如原文可能写“若已用药，需间隔5天以上”“注意区分零星发病与全田蔓延”，这些都可直接转化为追问点。  
3. **支撑后续多轮**：有了关键追问点，模型在对话中就能主动询问缺失信息，而不需要SOP提供复杂的条件逻辑。  
4. **不增加人工标注负担**：抽取时大模型能完成大部分工作，人工只需校验。  

### 完整示例（输入片段 → 输出）

**输入文本**（《甘蔗病虫害防治图谱》梢腐病节选）：

> 梢腐病主要发生在拔节期，高温高湿条件下易发。症状：心叶出现黄褐色小斑，后扩展为梭形或长条形，中央灰白色，边缘暗红。防治：发病初期用10%苯醚甲环唑1000倍液，每7天一次，连喷2次。注意：如已用过三唑类药剂，应轮换使用；阴雨天应抢晴施药。

**输出**：
```json
[{
  "title": "甘蔗梢腐病防控SOP",
  "scope": "甘蔗拔节期，高温高湿条件",
  "symptoms": "心叶出现黄褐色小斑，后扩展为梭形或长条形，中央灰白色，边缘暗红",
  "triggers": "高温高湿",
  "steps": ["发病初期用10%苯醚甲环唑1000倍液，每7天一次，连喷2次"],
  "precautions": "如已用过三唑类药剂，应轮换使用；阴雨天应抢晴施药",
  "verification": "",
  "clarification_points": ["是否已经用过三唑类药剂？", "近期天气是否多雨？"],
  "source": "甘蔗病虫害防治图谱"
}]
```

这样修改后，既可以继续用简单格式生成口语化问题（忽略 `clarification_points`），也为未来的多轮专家对话预留了空间.